mutation ProcessTokenDispenserAllocationTransaction(
  $tokenDispenserId: TokenDispenserId!
  $user: String!
  $signature: String!
) {
  processTokenDispenserAllocationTransaction(
    tokenDispenserId: $tokenDispenserId
    user: $user
    signature: $signature
  ) {
    ...tokenDispenserAllocation
  }
}
